# API calls now go through the Vercel proxy (same-origin) for Safari/ITP cookie compatibility.
# The rewrite rule in vercel.json proxies /api/* to the Render backend.
VITE_API_URL=

# Backend URL for Google OAuth and Chapa callbacks
CLIENT_URL=https://amana--fullstack.vercel.app
